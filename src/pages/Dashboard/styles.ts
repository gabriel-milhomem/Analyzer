import styled from 'styled-components';

import { Container } from '../../components/Tables/styles';
import { StyledHome, Menu } from '../Home/styles';

export const StyledDashboard = styled(StyledHome)``;

export const MenuDash = styled(Menu)``;

export const Body = styled(Container)``;
